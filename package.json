{
  "name": "ltc-recruitment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"const fs=require('fs'),cp=require('child_process');if(fs.existsSync('client')){cp.execSync('cd client && npm install && npm run build',{stdio:'inherit'});}else{cp.execSync('npm install && npm run build',{stdio:'inherit'});}\""
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0",
    "mongodb": "^6.5.0",
    "mongoose": "^8.3.1",
    "multer": "^1.4.5-lts.1",
    "node-cron": "^3.0.3",
    "nodemailer": "^6.9.13",
    "pdf-lib": "^1.17.1"
  }
}

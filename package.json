{
  "name": "ltc-recruitment",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm install && cd client && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^4.22.2",
    "mongodb": "^6.21.0",
    "mongoose": "^8.24.4"
  }
}

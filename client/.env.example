# ================================================================
# LTC RECRUITMENT PORTAL — CLIENT ENVIRONMENT CONFIGURATION
# ================================================================

# API URL Base Endpoint
# Leave empty ("") when deploying on Vercel under single-domain proxy rewrite.
# Specify full URL (e.g., https://api.ltc-recruitment.com) if client & server are hosted on separate domains.
VITE_API_URL=
